<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MyBricksApp.Core.Controls;assembly=MyBricksApp.Core"
             x:Class="MyBricksApp.MenuPage">
    <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <controls:ContentLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                <controls:ContentLayout.Content>
                    
                </controls:ContentLayout.Content>
            </controls:ContentLayout>
            <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                <Image Source="empty.png" WidthRequest="40" HeightRequest="40" HorizontalOptions="Center"/>
                <Label Text="No content found" HorizontalOptions="Center"/>
            </StackLayout>
        </Grid>
        <controls:AnimatedMenu MenuTitle="{Binding Title}" MenuIcon="{Binding TitleIcon}" MenuItems="{Binding MenuItems}" NavigateCommand="{Binding MenuNavigationCommand}"/>
    </Grid>
</ContentPage>
